# Sistema de Optimización de Red de Distribución

Proyecto de la Unidad 5 del curso de Logística: aplicación Flutter multiplataforma (Windows, Linux, Android) que decide **cuántos centros de distribución abrir, en cuáles sitios candidatos, y qué zona de demanda atiende cada uno**, minimizando el costo logístico total anual sujeto a un estándar de nivel de servicio.

Cubre los capítulos 13 y 14 de Ballou (5.ª ed.): ubicación de instalaciones y planeación/asignación acumuladas.

- **Fuente de verdad técnica:** [CLAUDE.md](./CLAUDE.md) — restricciones, esquema de base de datos, pseudocódigo de los nueve módulos del motor, fases de implementación y antipatrones.
- **Contexto académico:** [sistema-red-distribucion.md](./sistema-red-distribucion.md) — qué es el sistema, qué problema resuelve, anclaje teórico y cómo se conecta con el resto de proyectos del repositorio (pendiente de copiar desde la raíz del repo).
- **Paquete compartido:** [`paquete_geo_logistica/`](./paquete_geo_logistica/) — motor de ruteo OSRM, utilidades geográficas y selector de ubicación, extraídos de `sistema-optimizacion-rutas/`. Vive anidado dentro de este proyecto (no en la raíz del repo) para que la carpeta `sistema-red-distribucion/` completa sea autocontenida y se pueda entregar sola.

## Qué hace y qué no

**Hace:** agrega clientes en zonas de demanda, genera candidatos por centro de gravedad, consulta una matriz de distancias reales por carretera, corre heurísticas de ubicación discreta (ADD, DROP, intercambio, recocido simulado) sobre un modelo de costo de siete rubros, y entrega la curva de costo total contra número de almacenes con el mínimo marcado.

**No hace:** no es un ruteador de vehículos (eso es `sistema-optimizacion-rutas/`), no es un WMS ni un dimensionador de almacén (eso es `sistema-diseno-almacenes/`), no es un GIS, y no usa ningún solver MIP — todo el motor es heurístico, en Dart puro.

## Estado actual

**Fase 0 a Fase 9 completas.** Proyecto cerrado según su propio `CLAUDE.md` — quedan solo los tres pasos manuales del usuario descritos en la sección de empaquetado más abajo (push, secrets de GitHub, y confirmar el `.exe` en una Windows real).

- [x] `paquete_geo_logistica/` creado como paquete Dart local **anidado dentro de este proyecto** (no en la raíz del repo, para que la carpeta completa se pueda entregar sola como código fuente), consumido por dependencia `path: paquete_geo_logistica`. Copia desacoplada de `sistema-optimizacion-rutas/` (que **no se modificó**): `OsrmClient` ya no depende de drift/`AppDatabase`, sino de una interfaz `CacheRuteo` que la app implementa contra su propia tabla; `SelectorUbicacionScreen` ya no trae un centro por defecto propio, lo recibe como parámetro. Tests migrados (`geo_utils_test.dart`, `osrm_client_test.dart`, adaptado a `CacheRuteoEnMemoria`) pasan sin tocar sus aserciones, más un test nuevo de la interfaz. `flutter analyze` limpio, 22/22 tests en verde.
- [x] Proyecto Flutter `sistema-red-distribucion/` creado con soporte para `windows`, `linux`, `android`.
- [x] Dependencias instaladas y justificadas en `pubspec.yaml`: `paquete_geo_logistica` (path), `provider`, `drift`+`drift_flutter`, `path_provider`, `flutter_map`+`latlong2`, `http`, `crypto`, `fl_chart`, `pdf`+`printing`, `flex_color_scheme`+`google_fonts`+`lucide_icons_flutter`+`flutter_animate`, `share_plus`+`url_launcher`+`android_intent_plus`.
- [x] Estructura de carpetas de `lib/` y `test/` según la sección 9 de `CLAUDE.md`, con la nomenclatura `lib/ui/pantallas/` (no `lib/presentation/screens/` de la Unidad 3 — unificada hacia adelante).
- [x] `docs/fuentes_datos.md` creado (vacío, a completar cuando entren datos reales o del caso de estudio).
- [x] **Fase 1 — Esquema de base de datos.** Las quince tablas de la sección 6 en drift (`lib/data/local/database.dart`), `schemaVersion` 1, `PRAGMA foreign_keys = ON` explícito y cascada real hasta 2 niveles (`proyecto` → `escenario` → `memoria_calculo`/`escenario_almacen`/`escenario_asignacion`/`escenario_costo`/`punto_curva`, y `proyecto` → `cliente`/`zona_demanda` → `cliente_zona`). Invariante monetaria (céntimos enteros para dinero, metros/segundos enteros para distancia/duración, `double` solo en coordenadas/pesos/factores) respetada en todas las columnas. `CacheRuteoDrift` (`lib/data/local/cache_ruteo_drift.dart`) implementa la interfaz `CacheRuteo` del paquete contra `cache_ruteo`. Modelos de dominio (`lib/data/models/`) y repositorios (`lib/data/repositories/`) para las 15 tablas: CRUD completo en las de datos maestros (proyecto, cliente, zona_demanda, sitio_candidato, planta, parámetros de costo — 1 fila por proyecto); alta en bloque + consulta + baja en las de resultado calculado (cliente_zona, celda_matriz, escenario y sus 5 tablas hijas), que se recalculan enteras en vez de editarse fila por fila. `flutter analyze` limpio, 40/40 tests en verde — incluye el test de borrado en cascada exigido por la fase (eliminar un proyecto con todo su árbol cargado deja las 13 tablas relacionadas vacías) y el de invariante monetaria (`S/ 1234.56` → exactamente `123456` céntimos, ida y vuelta).
- [x] **Fase 2 — Datos de entrada y mapa.** Pantallas 1 (Inicio/Proyectos, fusionada con el CRUD de proyecto), 2 (Proyecto), 3 (Clientes: lista, formulario, importación CSV), 6 (Sitios candidatos), 7 (Plantas) y 8 (Parámetros de costo), todas en `lib/ui/pantallas/`, con `ProyectoActivo` (`lib/core/estado/`, `ChangeNotifier` vía `provider`) como estado del proyecto abierto. `SelectorUbicacionCampo` (`lib/ui/widgets/`) integra `SelectorUbicacionScreen` del paquete compartido en los tres formularios con coordenadas (cliente, candidato, planta), combinado con campos de texto editables — a diferencia de `sistema-optimizacion-rutas` (mapa-solamente), acá también se puede escribir la coordenada a mano, con validación de rango (CLAUDE.md exige rechazar latitud/longitud fuera de rango). Importador CSV (`lib/domain/importacion/importador_csv_clientes.dart`): detecta separador (coma/punto y coma/tabulador) y encabezado (mapea columnas por nombre, o asume orden fijo sin encabezado), rechaza filas malformadas una por una sin abortar el resto; UI de "pegar CSV" en vez de selector de archivos nativo (sin `file_picker`, dependencia no justificada todavía). Pantalla 4 de auditoría (`lib/domain/auditoria/auditor_datos.dart`): las seis reglas exactas de CLAUDE.md (coordenada fuera de rango, coordenada duplicada, demanda no positiva, cliente sin pedidos, candidato sin costo fijo, tarifa faltante), cada hallazgo con severidad y acción sugerida. `flutter analyze` limpio, 65/65 tests en verde — incluye los tests exigidos por la fase (importador con los tres separadores, con/sin encabezado, filas malformadas; las seis reglas de auditoría con caso positivo y negativo; widget test del formulario de cliente rechazando latitud 91 y longitud −181). **Verificado también corriendo la app real** (Linux, headless) — arranca sin errores, mismo patrón de warnings benignos ya visto en Fase 9 de `sistema-diseno-almacenes`; la captura de pantalla salió en negro por la misma limitación de Xvfb/Impeller de este sandbox, no por un defecto de la app (los widget tests sí ejercitan el árbol de widgets real).
  - **Diferido a la Fase 3, a propósito:** el botón "Generar por centro de gravedad" de Sitios candidatos queda deshabilitado (con aviso). M2 (CLAUDE.md sección 7) recibe *zonas de demanda* como entrada, y esas no existen hasta que M1 (Fase 3) las agregue — implementarlo ahora habría violado el orden de fases de la sección 0 ("no se salta a una fase posterior").
- [x] **Fase 3 — Agregación y verificación del límite de la matriz.** M1 (`lib/domain/motor/m1_agregacion.dart`): k-means geográfico ponderado por demanda con inicialización k-means++ y semilla explícita reproducible (`[REGLA]` sección 7), siempre por haversine (nunca ruteo), con manejo de clústeres vacíos (reubicación en el punto más lejano de su centroide, arreglo estándar de k-means) y `proponerK` (`min(maxCoordenadasPorConsulta − reservaCandidatos, nClientes)`). M2 (`lib/domain/motor/m2_centro_gravedad.dart`, ya no diferido): Weiszfeld de un centro y de múltiples centros (asignación al más cercano + recálculo hasta estabilizar), ahora sí conectado al botón "Generar por centro de gravedad" de Sitios candidatos — valida que existan zonas y parámetros de costo antes de ofrecerlo, y los candidatos sugeridos quedan con `origen = 'centro_gravedad'`, costo/capacidad en cero (los completa el usuario) y un aviso de que hay que verificarlos. `tool/verificar_limite_osrm.dart`: corrido de verdad contra el servidor demo público de OSRM (búsqueda exponencial + binaria, 11 peticiones reales, resultado documentado en [`docs/limites_osrm.md`](./docs/limites_osrm.md)) — **`maxCoordenadasPorConsulta = 100`**, verificado empíricamente, no supuesto, fijado en `lib/core/constantes.dart`. Pantalla 5 (`lib/ui/pantallas/agregacion/agregacion_screen.dart`): control deslizante de `k`, mapa con clientes y centroides de zona coloreados, y tabla con demanda/pedidos/error de agregación por zona; recalcular reemplaza las zonas del proyecto por completo (se borran y se reinsertan, nunca se editan a mano). `flutter analyze` limpio, 85/85 tests en verde — incluye los cinco tests exigidos por la fase (dorados A y B, C, D, E) más los de `proponerK`, M2 y el buscador del límite (probado con un "servidor" falso, sin red, para no depender de OSRM en cada corrida de tests).
- [x] **Fase 4 — Matriz de distancias.** `paquete_geo_logistica` gana `OsrmClient.obtenerMatrizAsimetrica(origenes, destinos)` (matriz `origenes.length × destinos.length` vía `sources`/`destinations` de OSRM, no la cuadrada completa) y `OsrmException.causa` (`CausaOsrmException`: `redNoDisponible` | `limitePeticiones` | `sinRuta` | `otro`), para que M3 distinga "no hay red" (respaldo válido) de un error real que debe propagarse — único cambio de esta fase al paquete, según lo previsto en su propio README. M3 (`lib/domain/motor/m3_matriz_distancias.dart`): función pura respecto a la base de datos — calcula, nunca escribe; trocea orígenes/destinos en bloques de `maxCoordenadasPorConsulta / 2` y solo consulta los bloques con al menos una celda faltante (agregar un candidato dispara solo su bloque, no la matriz entera); si la primera petición falla por falta de red, respalda en haversine × factor de circuidad el resto sin abortar; cualquier otro error (límite de peticiones agotado, `NoRoute`) se propaga sin guardar nada a medias, porque quien orquesta (`MatrizScreen`) solo persiste si `construirMatriz` termina con éxito. `CeldaMatrizRepository.insertarTodas` pasa a `INSERT OR REPLACE` para poder re-escribir un bloque completo sin violar el `UNIQUE`. Pantalla 9 (`lib/ui/pantallas/matriz/matriz_screen.dart`): estado de la matriz, proporción de celdas por fuente (osrm vs. haversine), progreso por bloque con tiempo estimado restante durante la construcción, y un interruptor de "modo sin conexión" explícito. `flutter analyze` limpio; paquete 28/28 tests, app 91/91 — incluye los cinco tests exigidos por la fase (troceado con servidor OSRM falso que devuelve distancias = producto de índices, verificando una matriz 25×30 completa y el número exacto de bloques; caché sin peticiones en la segunda corrida; caché parcial disparando solo los bloques del candidato nuevo; respaldo haversine×factor_circuidad exacto sin red; y 429 persistente propagando `OsrmException` sin devolver nada).
- [x] **Fase 5 — Costo y asignación.** Sin pantalla nueva, a propósito: la sección 9 de `CLAUDE.md` solo asigna esta fase a los tres módulos y la memoria de cálculo — las pantallas de resultado (12, 13) son Fase 7, y la que realmente decide qué candidatos abrir (Pantalla 10, Optimización) es Fase 6; construir una pantalla de "costo" ahora habría exigido inventar una selección manual de candidatos que Fase 6 va a reemplazar con heurísticas reales. **Gap de datos resuelto con el usuario antes de programar:** el pseudocódigo de M4 pide `distancia_planta_almacén`, pero la matriz de la Fase 4 solo conectaba orígenes con *zonas*, nunca planta→candidato. Se extendió `MatrizScreen` (Fase 4) para construir también esa segunda matriz bajo el mismo botón, reutilizando `construirMatriz` sin tocar su lógica (`DestinoMatriz` ahora acepta un `tipo` además de `'zona'` — cambio aditivo, con retrocompatibilidad verificada con los tests de la Fase 4 tal cual estaban). M7 (`lib/domain/motor/m7_agrupacion_riesgos.dart`): regla de la raíz cuadrada. M5 (`lib/domain/motor/m5_asignacion.dart`): sin restricción de capacidad, cada zona al almacén de menor `costo_salida + costo_manejo`; con restricción, heurística golosa por costo de oportunidad (`[REGLA]`: no óptima, documentado en su propia memoria) — una zona que supera el estándar de servicio queda igual asignada y además marcada en `zonasNoCubiertas` (nunca desaparece), y una zona sin capacidad disponible en ningún almacén abierto queda en `zonasSinAsignar` (nunca se cuela en un almacén excedido). M4 (`lib/domain/motor/m4_costo_total.dart`): los siete rubros, cada almacén abierto abastecido por su planta de menor distancia real; cada rubro se redondea a céntimos individualmente y el total es la suma de esos enteros (nunca hay diferencia de redondeo entre el desglose y el total); sin plantas cargadas, producción y entrada quedan en cero en vez de inventar un origen. Cada módulo devuelve sus propias filas de `FilaMemoria` (sin `escenarioId` todavía — no existe un escenario real hasta la Fase 6, que es quien las persiste). `flutter analyze` limpio, 100/100 tests en verde — incluye los seis tests exigidos por la fase: el dorado F (costo calculado a mano, céntimo a céntimo), la raíz cuadrada exacta (÷1=2 y ÷1=3), asignación al menor costo sin restricción, capacidad excedida sin colar ninguna zona de más, estándar de servicio sin hacer desaparecer zonas, y al menos una fila de memoria por rubro.
- [x] **Fase 6 — Heurísticas.** M6 (`lib/domain/motor/m6_heuristicas.dart`): ADD, DROP, intercambio de Teitz y Bart, recocido simulado (semilla explícita, temperatura inicial calibrada muestreando vecinos para ~50% de aceptación de empeoramientos, enfriamiento geométrico, para por iteraciones sin mejora) y enumeración exhaustiva (hasta 14 candidatos). Costo de una configuración = M5 (asignación) + M4 (costo total) compuestos en `EvaluadorCosto` (`lib/domain/motor/evaluador_costo.dart`), que carga todo en memoria una sola vez — ninguna consulta a drift dentro de un bucle de optimización (`[REGLA]` sección 10). Cancelación real: cada heurística es `async` y cede el control al event loop cada pocos pasos (`_puntoDeControl`) para poder revisar un `TokenCancelacion` compartido con la UI — al cancelar, la función lanza `BusquedaCancelada` en vez de devolver un resultado, así que quien orquesta (Pantalla 10) nunca llega a la persistencia. Pantalla 10 (`lib/ui/pantallas/optimizacion/optimizacion_screen.dart`): selección de método, `p` fijo o libre, semilla (recocido/intercambio), restricción de capacidad, progreso en vivo y botón de cancelar; al terminar con éxito persiste el `Escenario` completo (almacenes abiertos, asignación de zonas, costo por rubro y memoria de cálculo de M6+M5+M4 juntas) en una sola tanda, después de que la búsqueda ya devolvió un resultado válido. `flutter analyze` limpio, 106/106 tests en verde — incluye los cinco tests exigidos por la fase: el dorado L (ADD + intercambio iguala o queda a menos del 2% del óptimo exacto, en cinco instancias de 8 a 12 candidatos con semilla fija — en la práctica, todas las instancias generadas igualaron el óptimo exacto), DROP dentro del mismo margen, recocido simulado reproducible con la misma semilla, monotonía estricta del costo en cada paso de ADD, y cancelación sin resultado devuelto.
- [x] **Fase 7 — Curva y escenarios.** M8 (`lib/domain/motor/m8_barrido.dart`): para p=1..pMax, obtiene la mejor configuración de tamaño p (enumeración exhaustiva si hay ≤14 candidatos — óptimo exacto en cada punto, necesario para que el rubro de salida sea monótono; si no, ADD hasta p + intercambio) y calcula su costo con desglose completo; el mínimo de la curva es la recomendación (`[REGLA]` antipatrones: nunca se escribe el p óptimo a mano). Se ofrece como un sexto "método" en Pantalla 10 ("Barrido"), que además de persistir el `Escenario` de la mejor configuración, guarda un `punto_curva` por cada p evaluado. M9 (`lib/domain/motor/m9_comparador.dart`): función pura sobre un DTO `EscenarioDatos` (para poder comparar un `Escenario` persistido contra "la red actual", que nunca se guarda como escenario) — diferencia de costo por rubro, almacenes que abren/cierran, zonas que cambian de asignación, ahorro anual y variación del cumplimiento del estándar de servicio (recalculado desde las distancias de cada asignación, porque `escenario_asignacion` no guarda un booleano de cumplimiento). Pantalla 12 (`resultado_costos_screen.dart`): desglose por rubro con gráfico de barras (`fl_chart`, primer uso en este proyecto) y comparación contra los sitios candidatos marcados `esRedActual` (calculada al vuelo con M5+M4, ya que la red actual no es un escenario guardado); avisa en vez de comparar contra nada si no hay ninguno marcado o falta la matriz de esos sitios. Pantalla 13 (`curva_screen.dart`): línea de costo total contra número de almacenes con el mínimo resaltado, y barras apiladas por rubro en cada punto — solo tiene datos para escenarios generados con "Barrido" (los demás métodos evalúan una sola configuración, no un barrido). Pantalla 15 (`comparador_escenarios_screen.dart`): dos escenarios lado a lado vía M9. `flutter analyze` limpio, 114/114 tests en verde — incluye los cuatro tests exigidos por la fase: Test Q (forma de la curva — se construyó a propósito una instancia con tres clústeres de candidatos redundantes para forzar un mínimo interior real en p=3, verificado decreciente antes y creciente después), Test R (los siete rubros suman exacto el total en cada punto), Test S (salida no crece, inventario no decrece al aumentar almacenes) y Test T (el ahorro reportado es exactamente la diferencia de costos totales).
- [x] **Fase 8 — Mapa de resultados y visor web.** Paleta de territorios (`lib/ui/paleta_territorios.dart`): a diferencia de `paleta_rutas.dart` de la Unidad 3 (validada solo externamente), acá el contraste se verifica dentro del propio código con `deltaE()` (`lib/ui/color_contraste.dart`, CIE76 ΔE*ab completo: sRGB → lineal → XYZ D65 → Lab) — el generador prueba hues en pasos de 5° y solo acepta un color cuando su ΔE real supera `umbralContrasteMinimo = 12` contra la paleta base **y** contra todos los colores ya asignados en esa llamada (no solo por distancia de hue, que resultó ser un proxy no confiable: dos hues cercanos pueden tener ΔE muy distinto según saturación/luminosidad, y un recorte solo-por-hue puede quedarse sin hues válidos). Verificado empíricamente hasta 12 territorios: ΔE mínimo real 13.71, por encima del umbral. Pantalla 11 (`resultado_mapa_screen.dart`): mapa con un almacén por territorio (ícono coloreado), zonas coloreadas según su almacén asignado (gris `colorSinAsignarTerritorio` si ninguno), zonas fuera del estándar de servicio con borde rojo, y línea de asignación zona→almacén — el "no cubierta"/"sin asignar" se recalcula en pantalla contra `ParametrosCostoTable` en vez de guardarse en `escenario_asignacion` (mismo patrón que el comparador de la Fase 7). Exportación del visor (`lib/domain/export/exportar_visor_red.dart`): mismo mecanismo que `exportar_visor_web.dart` de la Unidad 3 (DEFLATE crudo + base64url en el fragmento de la URL, nunca en la query), pero a diferencia de ese, el color de cada almacén viaja ya resuelto en el payload en vez de derivarse por índice en JavaScript — portar el algoritmo de ΔE real a JS sin compartir código con Dart era un riesgo de desincronización más caro que las pocas decenas de bytes que ahorraba; `limitePracticoUrlVisor = 8000` caracteres, y si un escenario lo supera la app avisa (`ResultadoUrlVisor.excedeLimite`) en vez de generar un link roto. `visor-red/` (página estática, sin backend, HTML+CSS+JS puro): adaptación de `visor-web/` con el decoder de fragmento (`DecompressionStream('deflate-raw')` nativo del navegador) y renderizado Leaflet de almacenes/zonas/líneas rectas de asignación — sin geometría de carretera (a diferencia de `visor-web`, que sí pide rutas reales a OSRM), porque acá las líneas de asignación son rectas por definición, igual que en la propia Pantalla 11. `flutter analyze` limpio, 122/122 tests en verde — incluye los tres tests exigidos por la fase: Test U (hasta doce almacenes, ningún par de territorios adyacentes baja del umbral de contraste), Test V (la carga codificada y luego decodificada del visor devuelve exactamente los mismos almacenes y asignaciones, con un decoder Dart que mira el mismo formato que consume `app.js`), y Test W (treinta zonas caben bajo el límite práctico; un caso deliberadamente sobredimensionado de 2000 zonas devuelve `excedeLimite: true` y ningún URI). **Verificado también corriendo la app real** (Linux, headless bajo Xvfb): a diferencia de las fases anteriores, esta vez se encontró la causa real de que la ventana no apareciera en las capturas — GTK intenta el backend Wayland por defecto y falla en silencio en este sandbox sin compositor; forzando `GDK_BACKEND=x11` la ventana se crea y renderiza normalmente. Con eso confirmado: se creó un proyecto de prueba desde cero, se abrió el dashboard con la tarjeta nueva "Resultado — mapa" ya visible entre los módulos, y se entró a la Pantalla 11 sin ningún escenario calculado (muestra el estado vacío correcto, sin excepciones en el log).
- [x] **Fase 9 (parte A) — Exportación, caso de estudio e inducción.** Ficha técnica en PDF (`pdf_builder_red.dart`, mismo patrón que la Unidad 4 pero con las 5 columnas reales de este proyecto — sin `concepto`/`fuente`, que ese esquema no tiene — y con el título también escrito como metadato `/Title` del PDF, para poder verificar "contiene el nombre del proyecto" sin depender de extraer texto de un stream con glifos embebidos). Exportación CSV de clientes/zonas/candidatos/plantas/asignaciones (`exportar_csv.dart`). Proyecto portable en JSON (`proyecto_red_portable.dart` + `exportar_importar_proyecto.dart`): las 14 tablas propias del proyecto (todo menos `cache_ruteo`, que tampoco entra en el borrado en cascada de la Fase 1), con toda referencia cruzada viajando como **índice** dentro de su propia lista en vez de id — los ids de origen no sirven en el destino, y las claves foráneas de escenario→candidato/zona, celda_matriz→planta/candidato/zona, etc. se resuelven contra los ids nuevos que devuelve cada inserción. Exportación de "volumen por centro" para la Unidad 4 (`exportar_volumen_unidad4.dart`): releído `ProyectoPortable`/`entrada_calculo_screen.dart` de `sistema-diseno-almacenes` campo por campo (no inventado, `[REGLA]`) — un JSON por almacén abierto con `demandaAnual` = volumen real asignado y las filas de catálogo referenciadas por códigos reales del catálogo semilla de esa app (`EPAL`, `BAST-914`, `VIGA-1825`, `TRANSPALETA-MANUAL`, `C2`), que el importador de Unidad 4 resuelve localmente y descarta el resto de los campos — confirmado leyendo su `_resolverTarima`/etc. Caso de estudio de Pucallpa: reusa las mismas 19 coordenadas reales validadas de `sistema-optimizacion-rutas/lib/core/pucallpa_dataset.dart` (`lib/core/pucallpa_dataset_red.dart`), reinterpretadas como clientes + 1 planta; `tool/generar_semilla_pucallpa.dart` (correr con `flutter test`, no `dart run` — `database.dart` depende de Flutter vía `drift_flutter`) construye el proyecto completo con el motor real (M1 agrega en 8 zonas, M2 sugiere 4 candidatos por centro de gravedad, M3 consulta OSRM real para planta↔candidatos y candidatos↔zonas — 36/36 celdas confirmadas `fuente=osrm`, cero respaldo haversine) y vuelca todo a `assets/seed/semilla_pucallpa.json`; `sembrar_caso_estudio.dart` solo lee ese archivo al arrancar con la base vacía — **cero peticiones de red en el arranque real**. `docs/fuentes_datos.md` documenta el origen de cada dato del caso (coordenadas reusadas/verificadas, demanda escalada semana→año como asunción explícita, tarifas/costos genéricos sin fuente real). Inducción guiada (`lib/core/tour/`): mismo patrón que `sistema-diseno-almacenes` (pantalla propia, no overlay; `TourController` sobre `SharedPreferences`), 15 pasos cubriendo cada módulo real del sistema, con la paleta de territorios real de la Fase 8 como snapshot en el paso del mapa de resultados; se abre sola en el primer arranque (`ProyectosScreen.initState`) y queda disponible desde el ícono de ayuda del AppBar. Pantalla 16 (`exportacion_screen.dart`): junta todo lo anterior detrás de un selector de escenario, más "Importar proyecto" (`importar_proyecto_screen.dart`, mismo patrón sin selector nativo que la Unidad 4) y el enlace del visor web reutilizando `construirUrlVisorRed` de la Fase 8. `flutter analyze` limpio, 148/148 tests en verde — incluye los tres tests exigidos por la fase: Test X (exportar e importar un proyecto con las 14 tablas pobladas produce un proyecto idéntico en una base completamente separada), Test Y (el PDF se genera, tiene más de una página — verificado contando objetos `/Type /Page` reales, no un proxy — y contiene el nombre del proyecto), y Test Z (el caso de estudio sembrado permite correr una heurística de optimización completa con un cliente HTTP que lanza excepción ante cualquier petición, demostrando cero llamadas de red). **Verificado también corriendo la app real** (Linux, headless, `GDK_BACKEND=x11`): arranque limpio con el proyecto "Pucallpa (caso de estudio)" ya sembrado con sus 19 clientes reales y demanda correcta, inducción abriéndose sola en el primer arranque y quedando marcada como vista tras reiniciar el proceso, dashboard con la tarjeta "Exportación" nueva, Pantalla 16 con los botones dependientes de escenario correctamente deshabilitados (el caso sembrado no trae un escenario, a propósito), y una exportación CSV de clientes real de punta a punta confirmada leyendo el archivo resultante.
- [x] **Fase 9 (parte B) — Empaquetado real.** El título de la fase en `CLAUDE.md` lo menciona pero el bloque "Trabajo" de la sección 9 solo pide documentar comandos de compilación; el usuario pidió explícitamente sumar el ícono propio, la firma de release real de Android, el AppImage de Linux y el build de Windows por GitHub Actions — mismo alcance que ya tiene `sistema-diseno-almacenes`, replicado y verificado (no asumido) leyendo su `build.gradle.kts`, `Runner.rc`, `tool/appimage/build_appimage.sh` y el workflow reales.
  - **Ícono propio:** `assets/icon/icon.svg` (+ `icon_foreground.svg` para la capa adaptativa de Android) — un pictograma de red (nodo central + 4 nodos conectados, mismo lenguaje que `LucideIcons.network` ya usado en toda la app), blanco sobre el azul de `FlexScheme.shadBlue` (`#2563EB`, mismo tema visual que la app). Rasterizado a 1024px con `rsvg-convert`. `flutter_launcher_icons` (corrido a mano una vez con `dart run flutter_launcher_icons`) generó los mipmaps de Android con capa adaptativa y el `.ico` de Windows.
  - **Nombre visible:** `android:label` → "Red de Distribución"; `Runner.rc` (`ProductName`/`FileDescription`) → "Red de Distribución" / "Sistema de Red de Distribución".
  - **Android — firma de release real:** keystore generado localmente y en modo no interactivo (`keytool`, RSA 2048, validez 25 años) en `android/app/upload-keystore.jks` + `android/key.properties` (ambos cubiertos por `android/.gitignore`, verificado con `git check-ignore`, nunca se commitean). `build.gradle.kts` gana un `signingConfigs.create("release")` condicional: si `key.properties` existe usa la firma real, si no cae a la de debug (el build nunca se rompe por falta del keystore, ej. en un clon limpio). **Verificado de verdad:** `flutter build apk --release` (con `JAVA_HOME` apuntando a Java 21 — Java 26, el default de esta máquina, choca con un bug conocido de AGP/jlink en el build release) + `apksigner verify --print-certs` (certificado propio, no el de debug) + `aapt dump badging` (label, ícono y `applicationId` correctos).
  - **Linux — AppImage:** `tool/appimage/sistema-red-distribucion.desktop` + `tool/appimage/build_appimage.sh` (idéntico al de `sistema-diseno-almacenes`, parametrizado por `APP_NAME`; descarga `appimagetool` on-demand y corre con `--appimage-extract-and-run`, sin depender de FUSE). **Verificado con una captura real** bajo Xvfb headless con el fix `GDK_BACKEND=x11` (encontrado en la Fase 8) — el AppImage abre, carga el caso de estudio de Pucallpa ya sembrado y no tira ninguna excepción.
  - **Windows — GitHub Actions:** [`.github/workflows/build-releases-red-distribucion.yml`](../.github/workflows/build-releases-red-distribucion.yml) en la raíz del repo (`workflow_dispatch` únicamente), mismo patrón que el de `sistema-diseno-almacenes`: job `windows` bundlea el runtime de Visual C++ y empaqueta todo en un único `.exe` portable autoextraíble (SFX de 7-Zip); job `android` firma con la key de release real decodificada desde 3 GitHub Secrets (`ANDROID_KEYSTORE_BASE64`, `ANDROID_KEYSTORE_PASSWORD`, `ANDROID_KEY_PASSWORD`) si están configurados, o cae a la firma debug si no. Sin job de Linux (se cubre 100% local, ya verificado arriba).
  - **Pendiente, del lado del usuario (no ejecutado por Claude):** `git push` de este commit, dar de alta los 3 GitHub Secrets con el keystore en base64 y las contraseñas generadas localmente, disparar el workflow manualmente desde Actions, y confirmar que el `.exe` corre en una máquina Windows real (esta máquina es Linux, no puede probarlo) — e idealmente instalar el APK en un Android real.

## Requisitos previos

- Flutter SDK (canal estable) en el PATH.
- Para compilar el target Linux desktop: `clang`, `cmake`, `ninja-build`, `gtk3-devel`, `pkg-config`.
- Para Android: Android SDK.
- Para Windows: requiere Windows para compilar (ver Fase 9 de `CLAUDE.md` — se resuelve vía GitHub Actions, mismo patrón que los proyectos hermanos).

## Ejecutar en modo desarrollo

```bash
flutter pub get
flutter run -d linux   # o -d windows, o -d <device_id> para Android
```

## Generar código de drift

Cada vez que se modifique el esquema en `lib/data/local/database.dart` (a partir de la Fase 1):

```bash
dart run build_runner build --delete-conflicting-outputs
```

## Compilar para distribución (Fase 9)

**Linux — AppImage de un solo archivo:**

```bash
./tool/appimage/build_appimage.sh
# genera build/appimage/out/RedDeDistribucion-x86_64.AppImage
```

**Android — APK firmado:** requiere `android/key.properties` +
`android/app/upload-keystore.jks` (no se commitean — ver
`android/.gitignore`; generarlos localmente con `keytool` si no existen, o
usar los del CI vía los 3 GitHub Secrets de abajo). Sin ese archivo, el
build cae automáticamente a la firma de debug.

```bash
flutter build apk --release
# build/app/outputs/flutter-apk/app-release.apk
```

Si `flutter build apk --release` falla con un error de `jlink`/AGP en una
máquina con Java 26 instalado como default, forzar Java 21:
`JAVA_HOME=/usr/lib/jvm/java-21-openjdk flutter build apk --release`.

**Windows — `.exe` portable vía GitHub Actions** (no hay build local: se
necesita una máquina Windows para compilar el target `windows`):

1. `git push` la rama a GitHub.
2. En **Settings → Secrets and variables → Actions**, dar de alta:
   `ANDROID_KEYSTORE_BASE64` (`base64 -w0 android/app/upload-keystore.jks`),
   `ANDROID_KEYSTORE_PASSWORD` y `ANDROID_KEY_PASSWORD` (los mismos valores
   de `android/key.properties` local — sin estos, el job `android` del
   workflow cae a firma debug en vez de fallar).
3. **Actions → "Build releases - Red de Distribución (Windows + Android)"
   → Run workflow**, eligiendo la rama.
4. Descargar el `.exe` portable del artefacto `sistema-red-distribucion-windows-portable`
   y confirmarlo en una Windows real.
